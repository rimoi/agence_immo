<form method="post" action="{{ path('admin_country_delete', {'id': country.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimé ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ country.id) }}">
    <button class="btn btn-danger">Supprimé</button>
</form>
