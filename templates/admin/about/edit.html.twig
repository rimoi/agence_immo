{% extends 'admin.html.twig' %}

{% block title %}Modification de la page A propos{% endblock %}

{% block body %}

    <div class="py-2 text-center">
        <div class="d-flex">
            <div>
                <a href="{{ path('admin_about_index') }}" class="btn btn-dark">Retour</a>
            </div>
{#            <div class="pl-2">#}
{#                {{ include('admin/about/_delete_form.html.twig') }}#}
{#            </div>#}
        </div>
    </div>

    <div class="container pt-5">
        <div class="row justify-content-center">
            <div class="col-md-12">
                {{ form_start(form) }}
                <div class="card card-primary">
                    <div class="card-header">Modification de la page A propos</div>
                    <div class="card-body">
                        {{ include('admin/about/_form.html.twig'}) }}
                    </div>
                </div>

                {{ form_end(form) }}
            </div>
        </div>
    </div>

{% endblock %}
