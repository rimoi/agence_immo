{% extends 'admin.html.twig' %}

{% block title %}Nouvelle Page{% endblock %}

{% block body %}

    <div class="py-2 text-center">
        <a href="{{ path('admin_about_index') }}" class="btn btn-dark">Retour</a>
    </div>

    <div class="container pt-5">
        <div class="row justify-content-center">
            <div class="col-md-12">
                {{ form_start(form) }}
                <div class="card card-primary">
                    <div class="card-header">{{ block('title') }}</div>
                    <div class="card-body">
                        {{ include('admin/about/_form.html.twig') }}
                    </div>
                </div>

                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
