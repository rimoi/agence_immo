{% extends 'base.html.twig' %}

{% block title %}About{% endblock %}

{% block body %}
    <h1>About</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ about.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ about.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ about.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_about_index') }}">back to list</a>

    <a href="{{ path('admin_about_edit', {'id': about.id}) }}">edit</a>

    {{ include('admin/about/_delete_form.html.twig') }}
{% endblock %}
