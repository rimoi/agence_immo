<form method="post" action="{{ path('admin_tag_delete', {'id': tag.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir supprimé ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tag.id) }}">
    <button class="btn btn-danger">Supprimé</button>
</form>
